# Settings shared across all environments. Environment-specific values
# (service base URLs, per-env overrides) live in <env>.properties instead.
# Secrets (tokens, passwords) must NEVER be placed in any properties file -
# see README.md for the required environment variables.

http.connectTimeoutMs = 5000
http.socketTimeoutMs  = 10000

http.retry.maxAttempts = 3
http.retry.backoffMs   = 500
